spring:
  datasource:
    url: jdbc:h2:mem:shitlabtesttask
    username: sidey383
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: on